<% if !flash[:notice].blank? %>
  <div class="notice">
    <%= flash[:notice] %>
  </div>
<% end %> 

<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="teachers show">
  <h2>Teacher Subject</h2>

  <table summary="Teacher detail view">
    <tr>
      <th>Name</th>
      <td><%= @teacher.first_name %></td>
    </tr>
  </table>

</div>
