<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="Picture delete">
  <h2>Delete Picture</h2>

  <%= form_for(:picture, :url => {:action => 'destroy', :id => @picture.id}) do |f| %>

    <p>Are you sure you want to permanently delete this picture?</p>

    <p class="reference-name"><%= @picture.url %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Picture") %>
    </div>

  <% end %>
</div>